# Python packages required to run the code in this repository.
# Tested with Python 3.7 (the version reported in the manuscript, Section 5).
#
#   python -m pip install -r requirements.txt
#
# NOTE on CPLEX: docplex is the modelling layer only. It needs a CPLEX engine.
# The bundled Community Edition solves models up to 1000 variables / 1000
# constraints, which is enough for the smallest instances only. Reproducing the
# results reported in the manuscript requires a full CPLEX 22.1 installation
# (free for academics via the IBM Academic Initiative). After installing CPLEX
# Optimization Studio, install its Python API with:
#   python <CPLEX_STUDIO_DIR>/python/setup.py install

docplex==2.25.236
cplex==22.1.1.0
numpy==1.21.6
openpyxl==3.1.2
pandas==1.3.5
scipy==1.7.3
